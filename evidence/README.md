# Evidence directory

Only sanitized and explicitly public-safe receipts belong here.

Each future receipt must state:

- exact component and version;
- environment class without private identifiers;
- command or test path;
- expected and observed result;
- whether the result is live, offline, simulated or partial;
- limitations and expiry/last-verified date;
- confirmation that no secrets or employer/customer data are present.

## Current receipts

`runtime/kind-showcase-receipt.json` is generated by `scripts/kind-demo-smoke.sh`. It records an isolated Kubernetes reference run using safe prompts and a keyless model substitute. The trace ID is limited to that public reference environment.

`platform-factory-receipt.json` is generated by `scripts/verify-platform-factory.py`. It records the structural and sanitisation checks for the GitOps platform-factory slice and keeps every unexercised controller path marked `NOT_PROVEN`.

`runtime/platform-factory-kind-receipt.json` is generated by `scripts/platform-factory-kind-smoke.sh`. It records the five Kubernetes resources observed after a safe KRO `TeamEnvironment` reconciliation in the isolated reference environment. The Azure blueprint is never applied by that script.
